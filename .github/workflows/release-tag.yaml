name: Tag Release

on:
  release:

jobs:
  update-tag:
    runs-on: ubuntu-latest

    steps:
    - name: Run latest-tag
      uses: EndBug/latest-tag@latest
      with:
        ref: prod
        description: Latest release to prod
